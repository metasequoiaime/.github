# 维护交接清单

目标是让每个主要领域有第二位能够定位问题、评审改动和准备发布的人。这里规定交接记录的内容，不任命维护者、不授予权限，也不表示已经有人完成交接。当前人员与决策边界见 [GOVERNANCE.md](GOVERNANCE.md)。

## 按领域交接

| 领域 | 第二位维护者需要能够独立完成的工作 | 评审重点 |
| --- | --- | --- |
| Engine 输入与契约 | 从按键追到候选与学习；增加行为回归；核对三端消费版本 | 会话隔离、异步代次、共享格式、兼容路径 |
| Engine 数据与资源 | 从固定来源构建词库；核对清单与摘要；重放虚构用户词条 | 数据来源、许可证、可写用户数据、升级失败与回退 |
| Windows 产品 | 构建 DLL/Server；定位管道和候选窗问题；准备未发布安装包 | x86/x64、静态/动态 CRT、焦点、DPI、签名与 uiAccess |
| Apple 产品 | 构建 macOS 和 iOS；定位桥接、候选与文本提交问题 | 原生宿主生命周期、移动资源、权限、安装和更新 |
| Linux 产品 | 构建包并运行 IBus 冒烟；验证设置和词库安装 | D-Bus、吃键/提交、XDG 用户数据、取消与异步结果 |
| Docs / Web | 根据锁定源码核对指南；更新 Docs gitlink；验证下载元数据 | 正文单一来源、主分支与发布状态区别、更新不回退 |

每次交接选择一个领域和一项真实改动，沿用该仓库已有构建、测试和发布流程。不要为了演练发布生产版本、消耗签名次数或更改用户系统的输入源。

## 交接步骤

1. **定位与复现。** 记录平台、产品版本、源码提交、Engine gitlink、词库来源与摘要。先在干净基线重现问题，再说明修改影响的边界；示例只用虚构输入。
2. **独立评审。** 第二位维护者解释按键到提交或数据到安装的完整路径，指出修改的权威定义和消费者，核对拒绝/失败路径。现有维护者复核判断并记录遗漏。
3. **独立验证。** 从固定提交检出并执行当地要求的检查，附命令、环境、结果和 CI 链接。只读代码或构建通过不能替代原生宿主实测；未执行项目明确标为未验证。
4. **发布演练。** 准备产物、版本/来源清单、变更说明和回退步骤。签名配额不是限制（当前 3000 次/月），演练**要包含一次真实签名**并核验签出的产物——第二位维护者没亲手签过一次，就等于没验证过凭据在他手上能用。停的地方是**发布**：不创建用于演练的正式标签，不发布正式 release，产物保留在未合并分支与未发布状态。各平台以当前仓库发布规则为准；注意 Windows 的 `fix`/`feat` 合入 main 会自动推进发布，所以演练分支不要合。
5. **故障恢复演练。** 用独立的临时用户目录验证坏摘要拒绝、升级失败保留旧数据、回退保留新学习。涉及系统安装的演练使用明确的测试环境，记录清理方式。
6. **记录与确认。** 现有维护者填写结果、未覆盖部分和下一次实践任务。是否授予 Triage/Write 权限由既有治理流程决定；操作凭据不写入文档。

## 凭据托管

上面的演练停在实际签名与发布之前，副作用是：交接清单覆盖了所有**可以替代**的部分，唯独漏掉了**不可替代**的部分。真正会导致「这个人不在，事情就永远做不了」的不是评审能力，是凭据。

签名次数不构成限制（当前证书为 3000 次/月），所以「演练不做签名」这条不需要为配额让步：交接演练**应当**至少完成一次真实签名，否则第二位维护者从未验证过自己手上的凭据能不能用，交接就是纸面上的。不发布正式 tag 与不消耗签名是两件事，前者仍然要守。

因此这一节记录的是凭据的**存在与托管事实**，不记录凭据内容本身。凭据的值、指纹、口令一律不写进仓库、不写进 Issue、不写进 PR。

| 凭据 | 用途 | 丢失的后果 | 谁持有 | 在哪续期 / 重新申请 | 离线备份 |
| --- | --- | --- | --- | --- | --- |
| Windows 代码签名证书 | Authenticode 签名安装器与二进制；uiAccess 依赖它 | 无法发布带签名的安装包，候选窗无法浮于提权应用之上 | 待填 | 待填 | 待填 |
| Apple Developer ID | macOS 签名与公证 | 无法发布可直接打开的 macOS 包 | 尚未申请 | Apple Developer Program | 不适用 |
| `SPARKLE_ED_PRIVATE_KEY` | 签署 macOS 自动更新的 appcast | **所有已安装的 macOS 用户永久收不到自动更新。** 公钥已烧进出厂 `Info.plist`，无法在已发布的 bundle 里更换 | 待填 | 无法重新申请，只能换 key 并要求用户手动重装 | 待填 |
| `RELEASE_PLEASE_TOKEN` | 创建发布分支与 tag、通知官网刷新下载清单 | 发布流水线停摆 | 待填 | 重新签发 PAT 即可 | 不需要 |

Sparkle 私钥这一行和其他几行性质不同：其他凭据丢了可以重新申请，它丢了没有补救路径。**离线备份它是今天就该做的事，不需要等第二位维护者出现。**

每次交接演练时复核这张表：持有人是否仍然有效、续期日期是否临近、备份是否还能取出。填「待填」的行说明这件事还没落实，不要当成已完成。

## 可复制的记录模板

把以下记录放到对应评审或仓库文档中，链接回本清单。只有实际完成后才能填“通过”。

```text
领域：
现有维护者 / 参与交接者：
评审的 PR 与最终提交：
产品版本 / Engine 提交 / 数据来源与摘要：
独立定位与评审结论：
实际执行的命令、操作系统与工具版本：
CI / 测试 / 未发布产物证据：
升级失败与回退演练结果：
尚未覆盖的原生场景：
现有维护者复核结论：
下一项实践任务：
权限变更决定（无变更也记录）：
```

一次交接可以只覆盖评审或构建。把尚未完成的步骤保留在记录里；一个领域的演练结果不代表已经具备其他平台的发布能力。
